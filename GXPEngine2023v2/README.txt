This version of the GXP engine has Tiled pre-built into it, and you only have to replace a single bit of code to put your own tilemap in
You need to download Tiled for this to work, and youyr Tilemap must have no deadspace inbetween the tiles

The preset also has a small bit of collision and movement but nothing special, there are some extra functions in Mathf too curtesy of arda